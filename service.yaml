apiVersion: v1
kind: Service
metadata:
  name: timetable-service
spec:
  type: NodePort
  selector:
    app: timetable
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30008  # Specify a port between 30000-32767
